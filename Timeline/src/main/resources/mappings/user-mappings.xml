<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userDAO">
  <insert id="createAccount" parameterType="user">
    INSERT INTO timeline_user (username, password, name, email, profpic_path) 
    VALUES (#{username}, #{password}, #{name}, #{email}, 'default_image.png')
  </insert>
  <select id="login" parameterType="user" resultType="user">
		SELECT * FROM timeline_user WHERE username=#{username} AND password=#{password}
  </select>
  <select id="setProfpicPath" parameterType="user">
		UPDATE timeline_user SET profpic_path =#{profpic_path} WHERE username=#{username}
  </select>
  <select id="deleteAccount" parameterType="user">
		DELETE FROM timeline_user WHERE username=#{username}
  </select>
  <select id="viewAccount" parameterType="user" resultType="user">
		SELECT * FROM timeline_user WHERE username=#{username}
  </select>
  <select id="checkUsername" parameterType="user" resultType="boolean">
		SELECT * FROM timeline_user WHERE username=#{username}
  </select>
  <select id="updateAccount" resultType="user">
		UPDATE timeline_user SET username=#{username}, password=#{password}, name=#{name}, email=#{email} WHERE username=#{username}
  </select>
</mapper>